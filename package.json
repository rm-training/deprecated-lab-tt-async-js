{
  "name": "the-dom",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "json-server --watch data/db.json"
  },
  "author": "Ryan Morris",
  "license": "ISC",
  "dependencies": {
    "json-server": "^0.16.3",
    "semantic-ui-css": "^2.4.1"
  },
  "devDependencies": {
    "prettier": "^2.0.4"
  }
}
